{# vim: set syn=salt ts=2 sw=2 sts=2 et : #}

{%- set vm_name = salt["pillar.get"]("vm_names:templates:providers:onlykey") -%}

user:
  'dom0 or {{ vm_name }}':
    - templates.provides-onlykey
  '{{ vm_name }}':
    - common

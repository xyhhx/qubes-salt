{# vim: set syn=salt ts=2 sw=2 sts=2 et : #}

{%- set vm_name = salt["pillar.get"]("vm_names:templates:providers:gpg2", "provides-split-gpg2") -%}

user:
  'dom0 or {{ vm_name }}':
    - templates.provides-split-gpg2
  '{{ vm_name }}':
    - common
    - common.pkgs.sequoia-pgp

#!/usr/bin/env bash
set -axeuo pipefail

export DISPLAY=:0
user="$(id -nu 1000)"
factor="$(sudo -u "${user}" xfconf-query -c xsettings -p /Gdk/WindowScalingFactor)"
echo "${factor}"

# vim: set ft=sh :

